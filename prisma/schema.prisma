generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://f03b6a41bc4fe4035e4453264b946e79599fdb927e81a68d46c785283a27a2ef:sk_ZRix9vrwCr66dqfYTJn1a@db.prisma.io:5432/postgres?sslmode=require"
}

model Allergene {
  id          String              @id @default(cuid())
  nome        String              @unique
  descrizione String?
  icona       String?
  createdAt   DateTime            @default(now())
  updatedAt   DateTime            @updatedAt
  articoli    AllergeneArticolo[]
}

model Categoria {
  id        String     @id @default(cuid())
  nome      String
  ordine    Int        @default(0)
  attiva    Boolean    @default(true)
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  articoli  Articolo[]
}

model Articolo {
  id                 String              @id @default(cuid())
  nome               String
  descrizione        String?
  categoriaId        String
  prezzo             Float
  prezzoPromozionale Float?
  scadenzaPromo      DateTime?
  eSurgelato         Boolean             @default(false)
  eBestChoice        Boolean             @default(false)
  attivo             Boolean             @default(true)
  immagineUrl        String?
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  allergeni          AllergeneArticolo[]
  categoria          Categoria           @relation(fields: [categoriaId], references: [id])
}

model AllergeneArticolo {
  id          String    @id @default(cuid())
  articoloId  String
  allergeneId String
  allergene   Allergene @relation(fields: [allergeneId], references: [id], onDelete: Cascade)
  articolo    Articolo  @relation(fields: [articoloId], references: [id], onDelete: Cascade)

  @@unique([articoloId, allergeneId])
}

model SiteInfo {
  id                 String   @id @default(cuid())
  nomeLocale         String   @default("Il Nostro Ristorante")
  slogan             String?
  chiSiamoTitolo     String?
  chiSiamoTesto      String?
  chiSiamoImageUrl   String?
  logoUrl            String?
  faviconUrl         String?
  telefono           String?
  email              String?
  prenotazioniAttive Boolean  @default(true)
  heroTitle          String?  @default("Autentica Cucina Italiana")
  heroSubtitle       String?
  heroCTAText        String?  @default("Scopri il Menu")
  heroImageUrl       String?
  heroOverlayOpacity Float    @default(0.5)
  specialitaTitle    String?  @default("Le Nostre Specialità")
  specialitaSubtitle String?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
}

model SiteImage {
  id          String   @id @default(cuid())
  sezione     String
  titolo      String?
  descrizione String?
  url         String
  ordine      Int      @default(0)
  attiva      Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FooterInfo {
  id             String   @id @default(cuid())
  indirizzo      String?
  citta          String?
  cap            String?
  provincia      String?
  latitudine     Float?
  longitudine    Float?
  orariApertura  String?
  giorniChiusura String?
  facebookUrl    String?
  instagramUrl   String?
  twitterUrl     String?
  linkedinUrl    String?
  whatsappUrl    String?
  tiktokUrl      String?
  justeatUrl     String?
  deliverooUrl   String?
  glovoUrl       String?
  ubereatsUrl    String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model User {
  id        String      @id @default(cuid())
  email     String      @unique
  nome      String
  cognome   String?
  password  String
  ruolo     String      @default("admin")
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
  permessi  Permission?
}

model Permission {
  id                     String   @id @default(cuid())
  userId                 String   @unique
  puoGestireMenu         Boolean  @default(true)
  puoGestireFooter       Boolean  @default(true)
  puoGestireTemi         Boolean  @default(true)
  puoGestirePrenotazioni Boolean  @default(true)
  puoGestireDatiAzienda  Boolean  @default(true)
  puoGestireProfili      Boolean  @default(false)
  puoGestireAnalytics    Boolean  @default(true)
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
  user                   User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// Dati aziendali e legali
model CompanyData {
  id                  String   @id @default(cuid())

  // ==================== DATI AZIENDALI BASE ====================
  ragioneSociale      String?
  partitaIva          String?
  codiceFiscale       String?
  indirizzo           String?
  citta               String?
  cap                 String?
  provincia           String?
  paese               String?
  telefono            String?
  email               String?
  pec                 String?

  // ==================== DATA PROTECTION OFFICER (DPO) ====================
  dpoNome             String?
  dpoEmail            String?
  dpoIndirizzo        String?

  // ==================== PRIVACY POLICY ====================
  privacyPolicy       String?  // Se precompilato, sovrascrive il template automatico
  privacyEnabled      Boolean  @default(true)
  privacyUrl          String?
  privacyPolicyUpdate DateTime?

  // ==================== COOKIE POLICY ====================
  cookiesPolicy       String?  // Se precompilato, sovrascrive il template automatico
  cookiesEnabled      Boolean  @default(true)
  cookiesUrl          String?
  cookiesPolicyUpdate DateTime?

  // ==================== TIPI DI COOKIE UTILIZZATI ====================
  cookieTecnici       Boolean  @default(true)
  cookieAnalitici     Boolean  @default(true)
  cookieMarketing     Boolean  @default(true)

  // ==================== COOKIE BANNER ====================
  showCookieBanner    Boolean  @default(true)
  cookieBannerText    String?  @default("Questo sito utilizza cookie tecnici e analitici per migliorare la tua esperienza di navigazione. Cliccando su 'Autorizzo', accetti l'uso dei cookie.")
  cookieAcceptText    String?  @default("Autorizzo")
  cookieDeclineText   String?  @default("Annulla")

  // ==================== TERMINI DI SERVIZIO ====================
  terminiServizio     String?
  terminiUrl          String?

  // ==================== SCRIPT DI TERZE PARTI ====================
  thirdPartyScriptsEnabled Boolean @default(true)
  googleAnalyticsId     String?  // GA4 Measurement ID (es. G-XXXXXXXXXX)
  facebookPixelId       String?  // Facebook Pixel ID
  amazonTagId           String?  // Amazon Associate Tag ID

  // ==================== METADATA ====================
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
}

model Reservation {
  id        String   @id @default(cuid())
  nome      String
  cognome   String
  email     String
  telefono  String
  data      String
  ora       String
  persone   Int
  note      String?
  stato     String   @default("pending")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model AnalyticsEvent {
  id        String   @id @default(cuid())
  sessionId String
  eventType String
  pageUrl   String
  productId String?
  duration  Int?
  referrer  String?
  userAgent String?
  ipHash    String?
  country   String?
  city      String?
  timestamp DateTime @default(now())
}

model AnalyticsDaily {
  id                 String   @id @default(cuid())
  date               DateTime @unique
  totalVisits        Int      @default(0)
  uniqueVisitors     Int      @default(0)
  pageViews          String?
  avgSessionDuration Int      @default(0)
  hourlyBreakdown    String?
  topPages           String?
  productViews       String?
  conversionData     String?
  createdAt          DateTime @default(now())
}

model AnalyticsWeekly {
  id                 String   @id @default(cuid())
  weekStartDate      DateTime
  weekEndDate        DateTime
  totalVisits        Int      @default(0)
  uniqueVisitors     Int      @default(0)
  avgDailyVisits     Float    @default(0)
  bestDayOfWeek      String?
  bestHourOfDay      Int?
  topProducts        String?
  conversionInsights String?
  priceInsights      String?
  createdAt          DateTime @default(now())
}

model AnalyticsMonthly {
  id              String   @id @default(cuid())
  month           String
  year            Int
  totalVisits     Int      @default(0)
  uniqueVisitors  Int      @default(0)
  avgDailyVisits  Float    @default(0)
  bestDayOfWeek   String?
  bestHourOfDay   Int?
  avgWeeklyVisits Float    @default(0)
  growthRate      Float    @default(0)
  productInsights String?
  priceInsights   String?
  createdAt       DateTime @default(now())
}

model AnalyticsYearly {
  id               String   @id @default(cuid())
  year             Int      @unique
  totalVisits      Int      @default(0)
  uniqueVisitors   Int      @default(0)
  avgMonthlyVisits Float    @default(0)
  bestMonth        String?
  overallTrend     String?
  productInsights  String?
  priceInsights    String?
  createdAt        DateTime @default(now())
}
